services:
  - type: web
    name: b4u-user-portal
    env: python
    region: ohio
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:user_app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
